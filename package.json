{
  "name": "deinteam-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Centralized component system for the 6 Dein Team sites (DTH/DTL × .healthcare/.zuerich/.swiss)",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "typecheck": "turbo run typecheck",
    "ladle": "pnpm --filter @deinteam/components ladle"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
